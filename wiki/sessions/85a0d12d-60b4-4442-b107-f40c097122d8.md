---
tags: [session, mvp, mcp, recall, autopilot]
---

# Session: MVP loop closed — eight commits from prompt embed to autopilot

**2026-07-28 17:02–22:21 · project memoria**

One long sitting closed the full capture → consolidate → curate → recall loop. Commits, in order:

- **`6ea9c87`** — prompts embedded-only, overridable by a file in `~/.config/memoria/` ([[decisions/0004-embedded-prompts-with-file-override]]). The refactor left stale materialized copies on the dev machine → [[gotchas/stale-prompt-overrides]].
- **`5c50d83`** — `setup` command, systemd cron timer, `process --all` and `--inspect` (pre-existing working-tree changes committed as one feature) → [[concepts/consolidation-pipeline]].
- **`fca4954`** — `memoria search`: text / `#tag` over the wiki. Human-only by explicit decision: "This command will be used only by humans. We will create a small mcp later to give the llms this options too."
- **`0643004`** — `memoria run`: native resume (`claude --resume <sid>` / `codex resume <sid>`) when the digest's `client:` frontmatter matches the launched harness, one-sentence digest-handoff prompt otherwise. Installed hook commands now carry `--client <name>` so capture records the harness, and `run` became the first-ever reader of `.memoria/sessions.md` (`RFC3339 - SID - NAME` lines) → [[concepts/recall-and-run]].
- **`0eca8b2`** — bootstrap writes the `<!-- memoria:start --> ... <!-- memoria:end -->` recall block into the project's AGENTS.md (creating it if missing), plus a CLAUDE.md shim; the block is repaired/refreshed on re-run. Dogfooded on the memoria repo itself.
- **`5f54f75`** — MCP server on the official `modelcontextprotocol/go-sdk v1.6.1` ([[concepts/mcp-server]]): six tools, registered by init — claude-code via the top-level `mcpServers` key in `~/.claude.json`, codex via a `[mcp_servers.memoria]` table in `~/.codex/config.toml`. Consolidation schema converged on `{path,title,tags,body_markdown}` with a shared `renderPage` used by apply/seed/digest/write_page; `sessions/` became a valid wiki dir; deletes move pages to `wiki/trash/`.
- **`c97a8a4`** — README refreshed to document setup, cron, lint, search, run and the MCP server.
- **`25ab844`** — `auto_apply` autopilot, default off ([[decisions/0005-auto-apply-is-opt-in]]). Session end spawns a detached `process --foreground` (logf-only, hook still never blocks); skipped while the project's single job slot is busy; the per-session digest job is deliberately *not* spawned too — the batch prompt already writes `sessions/<sid>.md`, so a second LLM call would be a duplicate.

Design note worth keeping: the recall gap was analyzed as three layers — capability (local agents can already grep the wiki), discovery (does the agent know it exists), behavior (does it check before deciding). The AGENTS.md pointer block solves discovery + behavior for local agents; MCP adds tag search and structured access on top; a skill was judged unnecessary.

Loose ends at session close: `.gitignore` (`.memoria/` line) and the untracked `wiki/` folder still uncommitted; module path mismatch blocks `go install` → [[gotchas/module-path-mismatch-breaks-go-install]]; `v0.1.0` not tagged; the ollama processor is still a placeholder; the dev machine needs one `memoria init <client>` re-run to pick up `--client` hooks and MCP registration.

The whole MVP, first commit (2026-07-27 22:59) to the README commit (2026-07-28 22:03), was estimated at roughly 14–16.5 hours of hands-on work.