---
tags: [session, handoff, run, multirepo]
---

# Session: handoff made ask-first — `6414c80` — plus multirepo assessment

**2026-07-30 09:23 · project memoria · claude-code · commits `6414c80` + `31b7251` · title `fix-resumed-agent-handoff`**

## Part 1: the handoff packet stops auto-continuing

Bug report with screenshot: the user quit a claude-code session (after `/git-commit` ran fine) and resumed it under codex. A claude-code subagent had written "commit the wiki changes too" — in the user's words "this was not a request for permissions, not a suggestion, i never saw this message, it felt more like a internal anotation" — and codex "understood this as a pending thing and decided to execute this (taking 5 min)". The requirement: the resuming agent "should not start working right the way" — it should warn that something looks pending and ask whether to continue.

Explore recon pinned the mechanics (full failure analysis in [[gotchas/subagent-stop-promoted-to-user-request]]): `renderEvent` in hook.go is the single source of every `@…` digest line; `buildHandoff`'s "Last reported state" scan treated `@stop ` and `@subagent-stop ` as equivalent and purely positional, so a trailing subagent note beat the main agent's real final report; `@session-end reason:` is recorded verbatim and read by no code path; and `memoria_recall` shared the same lead selection.

Fix, all in `buildHandoff` (run.go): the last main `@stop` always wins the lead; a `@subagent-stop` is used only when the session has no `@stop` at all, labeled "internal subagent note — not a user request"; the footer became ask-first — "Do NOT start working yet. First report the current state in 1-3 lines … then WAIT for the user to say how to proceed"; the header ground rule now ends "report it briefly, and wait for the user's go-ahead". The existing packet test was extended to replay the incident's exact tail (`@stop` → `@subagent-stop` → `@session-end reason: prompt_input_exit`) and assert the old auto-continue footer is gone; new `TestRunHandoffLeadSubagentFallback` covers the labeled fallback. Deliberately skipped: interpreting `session-end` reasons — unneeded once resume always asks first. `/git-commit` landed **`6414c80`** — `fix(run): make handoff packet ask-first and prefer main @stop as lead` — staging run.go + run_test.go only; 4 auto-generated wiki files were left unstaged again ([[gotchas/auto-apply-rewrites-wiki-mid-session]]).

## Part 2: will memoria work on a multirepo folder?

Second request: eparts is many separate microservice repos under one parent folder, treated as one project — will memoria work there? Two Explore recons mapped project matching, every git shell-out, registration, and how shared state is keyed; the findings and verdict are distilled into [[concepts/multirepo-parent-project]]. After the user clarified the real workflow (worked as a monorepo, sessions at the parent, unversioned wiki acceptable "but it must have a warning"), the verdict: parent registration works today for that workflow, with three small adaptations proposed — a non-repo bootstrap warning, best-effort child-repo git checkpoints in `buildHandoff`, and a visible seed-skip warning. The session's final line, "Implement these 3?", got no captured reply.

Sibling digest in this batch: `019fb2fd-b4c7-7db3-adf2-f909c924dba8` (codex, 09:56:45) holds a single line — `@session-end reason: other` — started and ended the same second with zero events. This session's own digest carries no `@session-end`; it reached consolidation via the implicit-end path ([[gotchas/implicit-session-end]]).

## Incarnation 2: the multirepo adaptations committed — `31b7251`

The session reopened later the same morning (10:01–10:03) as incarnation 2 (`e112664f…-2.md`, `continues_from` the processed digest — the reopen mechanic in [[gotchas/implicit-session-end]]). The digest holds a single `/git-commit`: after `git status && git log -3 --oneline`, five files were staged and committed as **`31b7251`** — `feat(cmd): support multirepo parent projects with warnings and per-repo checkpoints` — `cmd/memoria/bootstrap.go`, `bootstrap_test.go`, `run.go`, `run_test.go`, `seed.go`. That file set lines up one-to-one with the three adaptations Part 2 left proposed: bootstrap warning → bootstrap.go, per-repo handoff checkpoints → run.go, seed-skip warning → seed.go. The implementation edits themselves appear nowhere in capture — the incarnation opens with `/git-commit` and the work already in the tree. Per the stop message, auto-generated wiki changes (3 modified + 4 untracked pages) were again deliberately skipped as "consolidation output, not part of this feature" ([[gotchas/auto-apply-rewrites-wiki-mid-session]]). Session ended with reason `prompt_input_exit`. Current multirepo behavior: [[concepts/multirepo-parent-project]].