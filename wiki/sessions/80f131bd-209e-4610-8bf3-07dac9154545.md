---
tags: [session, init, setup, multi-agent]
---

# Session: multi-agent init/setup shipped — `7128a3d`

**2026-07-29 21:55–22:23 · project memoria · claude-code · commit `7128a3d`**

Request: improve `init` and `setup` — install hooks+MCP on many code agents in one command (claude code + codex), let `setup` add an agent without removing an existing install, and record in the config file which agents have memoria set up. Run at `/model opus-5 /effort xhigh`, deliberately in parallel: "While another claude is reflecting on a hard task" — the handoff-packet session [[sessions/e915be64-f9af-4a50-a147-8a4831159754]] was working the same tree at the same time.

**Recon** (Explore agent) mapped the install surface before editing — durable findings folded into [[concepts/init-setup-multi-agent]]:

- All per-agent knowledge lived in `installClientHooks` plus ~6 scattered switch/map sites; the config had no record of installed agents, so nothing could answer "which agents did the user install into?" without parsing the agents' own settings files.
- `setup`'s doc comment said "Hooks stay init-only" — it had no client handling at all.
- No uninstall exists for hooks or MCP registration; the only removal path is the cron timer.

**Implementation** (Plan agent first, then edits to tui.go, config.go, hooks.go, init.go, setup.go, help.go plus both test files):

- `selectMulti` checkbox multi-select in tui.go — a stubable var mirroring `selectOption`.
- `Clients []string` (`clients:`) config field; `recordClients` merge-dedups into it, best-effort on save error.
- `init` takes multiple positional clients or `--client claude-code,codex`; interactive TTY shows the multi-select; fail-fast validation before any install (`init good bad` installs nothing); the `claude` alias is normalized so config never stores `claude`.
- `setup --client codex` adds an agent surgically with no prompts; interactive setup gains a first step printing installed clients and multi-selecting missing ones; `detectClients` backfills pre-feature installs by scanning the two hook files.

Tests: 9 new, 1 updated (`TestInitNonTTYSkipsProcessor` — the config file is now created to record clients); full suite green plus a temp-HOME smoke test of both agents' files and the `clients:` list.

**Cross-session friction**: mid-session `go test` failed on the *other* session's in-progress `run_test.go`, which referenced `gitCheckpoint` before it was written — the failure was theirs, not this session's. `ad5d206` landed first with no conflict; `/git-commit` here then committed the 8 files as `7128a3d`, closing the "multi-client work left unstaged" loose end recorded in [[sessions/e915be64-f9af-4a50-a147-8a4831159754]]. The session ended with reason `prompt_input_exit` and was resumed 90 seconds later under codex via the new handoff packet ([[sessions/019fb0a0-48d0-71f1-954f-7036553b2133]]).