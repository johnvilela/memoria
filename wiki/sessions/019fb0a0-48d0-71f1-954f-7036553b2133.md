---
tags: [session, handoff, codex, run]
---

# Session: handoff packet dogfooded — codex resumes the claude-code session

**2026-07-29 22:25–22:28 · project memoria · codex**

First captured real-world run of the cross-harness handoff packet from `ad5d206` ([[concepts/recall-and-run]]): the multi-agent init session [[sessions/80f131bd-209e-4610-8bf3-07dac9154545]] ended under claude-code at 22:23:55, and 90 seconds later this codex session started with the packet as its single user prompt — no LLM pass in between, built straight from the still-pending digest.

The packet observed in the wild matches the design: "You are RESUMING an in-progress coding session … NOT starting a new task", source harness named (claude-code), ground rules that listed tool calls already ran and the checkout is authoritative, a git checkpoint (`HEAD: 7128a3d`, `Worktree: clean`), the session history oldest-first with the full digest path named, and a Continue footer quoting the last `@stop`.

Codex behaved exactly as the packet intends — the failure it was built to fix ("codex tought it was to execute some task and not to just resume", per [[sessions/e915be64-f9af-4a50-a147-8a4831159754]]) did not recur. One verification command (`git status --porcelain=v1 -b && git show -s HEAD`), then the report: handoff intact, worktree clean, HEAD `7128a3d`, local `main` three commits ahead of `origin/main`, requested work complete, no remaining implementation step.

Follow-up task in the same session: add `wiki/.obsidian/` to `.gitignore`. Done, with the caveat that already-tracked Obsidian files stay tracked until explicitly removed from the index. (Capture quirk: the user's abandoned partial prompt "can you add the .ob" landed as its own `@user-prompt` line — hook capture has no edit awareness.)

Session ended with reason `other`.