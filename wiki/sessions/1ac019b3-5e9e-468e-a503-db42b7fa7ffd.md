---
tags: [session, debugging, queue, filter, canonical-paths]
---

**2026-08-01 17:45–20:47 · project memoria · claude-code**

## Problem report

Three large sessions from the motolink project over July 30–31 ("gigantic" with 3 features) were not appearing in the wiki despite consolidation runs. Screenshot showed "no valid pages to process" errors. Queue analysis showed the sessions stuck for 3+ hours, dragged through multiple cron cycles.

## Root causes diagnosed

### 1. Stale prompt override killed the consolidation output

`~/.config/memoria/wiki-prompt.md` — an old materialized file from Jul 28 (1446 bytes) — was shadowing the embedded default (4785 bytes). The override was missing sections added since:
- No "compile into 1-5 page updates" framing
- No fallback to return "only the episodic session page" when a session yields no insight — the line that stops empty arrays
- No OUTPUT FORMAT section (missing `<think>` ban, "first char must be `{`")

Result: empty digests were reaching the LLM. The model correctly returned `{"pages":[]}`. The system treated zero pages as hard failure.

### 2. Empty digests weren't filtered — queue entry never removed

`c589857b-...-3.md` (397 bytes, pure bookends): `@session-start source: resume` + `@session-end reason: prompt_input_exit`. Zero events. Sent to LLM, got `{"pages":[]}`, status turned red, notification fired. But `process.go:295` had no cleanup path — the queue entry stuck and re-failed on every `session-end` hook and every cron firing, poisoning any batch it rode in.

Same pattern at 12:15 with `55eed586` (335 bytes). Both lasted 3 hours until this session manually intervened.

### 3. Incarnation suffix leaked into page names, breaking readers

`c589857b-...-2.md` (60,601 bytes, your 3 features) was processed at 15:28 under the stale prompt → 1 page written. But the processor named it `sessions/c589857b-...-2.md` (including incarnation suffix). Readers (`memoria run`, `run.go:247`) hardcode `sessions/<session_id>.md` (bare id), so the page was invisible to the handoff system.

### 4. Session pages existed but on wrong branches

`wiki/sessions/c80486cf-...md` (Jul 31 19:09 session) was committed in `86b2198` but only to `fix/invalidate-invites-on-shift-removal`. Working branch was `feat/work-hours`. Unmerged = invisible.

## Fixes implemented

### 1. Retired stale prompt overrides

```bash
mv ~/.config/memoria/wiki-prompt.md ~/.config/memoria/wiki-prompt.md.bak
mv ~/.config/memoria/seed-prompt.md ~/.config/memoria/seed-prompt.md.bak
```

Both were outdated copies (wiki-prompt by 3 sections, seed-prompt by 1 bullet). Now fall back to embedded defaults.

### 2. Guard: filter empty digests before processor

**`cmd/memoria/process.go`** — new functions:

- `digestHasContent(body string) bool` — checks if digest has any event line other than `@session-start` / `@session-end`. Returns false for prose mentioning those strings.
- `retireEmptyDigest(proj, digest string)` — archives the digest to `processed/` and removes its queue entry.
- Wired into `collectEnded()` (called by all three LLM paths: `detachProcess`, `processAll`, `generateProposal`). One guard point, three callers.

**Tests** (new): `TestDigestHasContent` (edge cases including prose falsely containing `@session-start`), `TestCollectEndedRetiresEmptyDigests`, `TestProcessEmptyDigestOnlyIsNotAnError` (verifies processor stub errors if invoked — proves no LLM call).

Verified: Red (empty digests were passing through), then green.

### 3. Canonicalize session page paths

**`cmd/memoria/process.go`** — new function:

`canonicalizeSessionPaths(pages []wikiPage, sids []string) ([]wikiPage, []string)` — rewrites `sessions/<sid>-N.md` → `sessions/<sid>.md` by reading the digest's frontmatter `session_id` (not filename, to avoid UUID collision: a UUID can legitimately end in `-400060889612`). Drops orphan pages matching no known sid in the batch. Reports collisions when two incarnations target one path — the drop is reported, not silent.

Called in `generateProposal` before `validatePages`. Left `validatePages` untouched (it's shared with lint's fix pass, which edits session pages outside any batch — strict dropping there would break it).

**Tests** (new): `TestCanonicalizeSessionPaths` (includes the digit-tailed UUID case), `TestCanonicalizeSessionPathsDropsCollision`, `TestProcessPinsSessionPageToSessionID` (end to end).

### 4. Recovered motolink's session

- Cherrypicked `86b2198` (session page `c80486cf`) onto `feat/work-hours` as `8d432d6`.
- Re-queued the 60KB session and re-ran consolidation with the embedded prompt.
- Produced proper wiki page `sessions/c589857b-...-2.md` (6287 bytes) covering all three features: `/gestao/ponto` day-view redesign, `WorkHourReset` model + `resetDay` flow, rolling-45-day seed changes. Includes the load-bearing invariant and migration name.

## Code changes

- `process.go`: +119 lines (guards + canonicalization + `batchSessionIDs` helper)
- `process_test.go`: +166 lines (4 new tests, 100% green under `go test -race`)
- Binary rebuilt and installed to `~/.local/bin/memoria` (v0.7.0)

**Not committed**: The diff exists, staged set ready (`scripts/test.sh` green, gofmt clean).

## Outstanding

**Fix 2 (erosion prevention)** — not built. When incarnation N overwrites N-1 on the same path, the replay showed a loss: incarnation 1's 14KB page → incarnation 2's 6287 bytes. Fix 2 would be a prompt instruction to extend existing session pages rather than replace when `continues_from` is present. Deferred as soft guidance; the hard version (Go-side append) fights the "processor returns whole page" contract. Candidate for follow-up if erosion is a real pain.

**The motolink orphan** — `wiki/sessions/c589857b-...-2.md` (the stray page from the 15:28 run under the stale prompt) still exists. Once canonicalization lands, that file is an orphan. Fold it by hand or leave as history.

Related: [[gotchas/empty-digests-poison-queue]], [[gotchas/incarnation-suffix-session-pages-invisible]], [[decisions/0008-filter-empty-digests-before-processor]].