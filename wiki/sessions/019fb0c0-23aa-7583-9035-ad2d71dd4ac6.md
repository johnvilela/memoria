---
tags: [session, handoff, wiki, git-commit, recall]
lastUsed: 2026-08-31
---

# Session: wiki committed as `147ed73` — packet hands the titles session to codex

**2026-07-29 23:00–23:03 · project memoria · codex · commit `147ed73`**

Third real-world run of the `ad5d206` handoff packet, again claude-code → codex: the titles session [[sessions/7facd470-c6e9-489b-b490-58832dabc6e2]] had ended twice with "commit the wiki files too" unactioned, and this codex session launched with the packet as its single prompt — git checkpoint `HEAD: c478df6`, worktree dirty with 9 files, the Continue footer led by exactly that pending request.

First sighting of the packet's byte budget firing on real data: the history section carried "_(14 older events omitted to fit — read the files above for full history)_", with the full processed-digest path named, exactly the oldest-drop-first design in [[concepts/recall-and-run]].

## The commit

After reading the codex `scoped-commit` skill (`~/.codex/skills/scoped-commit/SKILL.md`), the wiki index and the tree state, then reviewing the full wiki diff and the five new pages (`git diff --check` included), the nine files were staged and committed as **`147ed73`** — `docs(wiki): capture recent session knowledge`:

- 4 updates: `wiki/concepts/architecture-overview.md`, `wiki/concepts/recall-and-run.md`, `wiki/concepts/session-capture.md`, `wiki/index.md`
- 5 new pages: `wiki/concepts/init-setup-multi-agent.md` plus the session pages `019fb0a0-48d0-71f1-954f-7036553b2133`, `6d290df9-cc9e-4703-bcc4-2143eba007aa`, `7facd470-c6e9-489b-b490-58832dabc6e2`, `80f131bd-209e-4610-8bf3-07dac9154545`

The commit also corrected the digest prompt reference to the moved `cmd/memoria/prompts/` folder. Worktree clean after.

## Recall dogfood at close

The user asked "can you remenber what we did on this session, please?". The capture then shows a 30-second `sleep`, followed by a git status check confirming that this session's **own** wiki page (`wiki/sessions/019fb0c0-....md`) already existed on disk, uncommitted — the answer was a link to that page, noting it records commit `147ed73`, the nine wiki files, the corrected prompt path, and the inherited context. "What did we do?" was answered by the wiki itself, written while the session was still running. (The digest records no command that wrote the page — only Write/Edit/Bash land in capture, so a background or MCP-driven write leaves no trace.)

Sibling digest in this batch: `39085d48-9f55-4f8c-bd38-1cb5614c664e` (claude-code, 22:57:48–22:57:54, start `startup`, end reason `resume`) captured zero events yet carries `title: memoria-55` frontmatter — the `c2e7e3a` title capture stamps even a six-second empty session.

Session ended with reason `other`; the session page itself was still uncommitted at close.