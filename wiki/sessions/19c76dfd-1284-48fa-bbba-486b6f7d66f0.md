---
tags: [session, bootstrap, global-capture, planning]
---

# Session: `--global`/`--path` bootstrap flags requested — recon only

**2026-08-26 20:50–20:58 · project memoria · claude-code**

Feature request, with an explicit "ask me anything to refine this task" that was never answered in this session: add two new flags to `memoria bootstrap`.

- **`--global`**: memoria captures every session in every folder, not just registered projects — a shift away from the current opt-in-per-project capture model ([[gotchas/hooks-global-capture-opt-in]]).
- **`--path <dir>`**: where global capture writes its wiki. The user's own prompt was resent mid-session with one correction — the target folder "doesn't need to be a git repo" (the first phrasing said "does need to be a git repo"). With no `--path`, global capture writes into the memoria folder itself and uses git to track the changes locally.

## Recon only — nothing implemented

The session was pure investigation: extensive reads plus two Explore subagent stops ("Reading commit.go and help.go", "Reading processAll in process.go"), covering:

- Project registration and routing — grepped `matchProject`/`wikiRootFor`/`projectAt` across `digest.go`, `mcp.go`, `lint.go`, `search.go`, `run.go`, `status.go`, `queue.go`
- Wiki path validation (`readWiki`, `validatePages`, `warnReservedDirs`/`reservedDirs`) and the `_global` reserved top-level name already in the wiki ([[concepts/architecture-overview]])
- `process.go`'s `processAll` and the consolidation flow ([[concepts/consolidation-pipeline]])
- Queue/status file paths (`queuePath`, `statusPath`, `runLogPath`, `logf`)
- `wiki/concepts/multirepo-parent-project.md`, read directly — the closest existing precedent for one wiki spanning more than a single registered folder ([[concepts/multirepo-parent-project]])
- `bootstrap_test.go`'s existing test names, `~/.config/memoria/config.yaml`, `~/.claude/settings.json` hook config, `AGENTS.md`'s bootstrap section, and `wiki/decisions/0010-wiki-auto-commit-is-opt-in.md`

A third Explore agent, "Explore memoria bootstrap flow", was launched and then stopped by the user before it finished. No file was edited, no design was settled, and the digest holds no answers to the agent's clarifying questions — the session ended with `prompt_input_exit` right after the background agent was killed.

## Status

Requested but unbuilt: `bootstrap --global` (capture everywhere, off the opt-in-per-project model) and `bootstrap --path <dir>` (target folder for global capture's wiki; not required to be a git repo; defaults to the memoria folder with local git tracking). No open design questions were resolved.